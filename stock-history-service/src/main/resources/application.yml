server:
  port: 8083
  
spring:
  datasource:
    url: jdbc:postgres://localhost:5434/${STOCK_HISTORY_DB}
    username: ${DB_USER}
    password: ${DB_PASS}
  jpa:
   hibernate:
     ddl-auto: update
   show-sql: true

kafka:
  bootstrap-servers: localhost:9092